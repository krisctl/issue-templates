name: 🚀 Feature Request
description: Suggest an idea for this project
title: "(short issue description)"
labels: [feature-request, needs-triage]
assignees: [chimmy-changa] #TODO: Update to Prabhakar's github ID
body:
  - type: textarea
    id: description
    attributes:
      label: Describe the feature
      description: A clear and concise description of the feature you are proposing.
    validations:
      required: true

  - type: textarea
    id: use-case
    attributes:
      label: Use Case
      description: |
        Why do you need this feature? For example: "It's time consuming to always do..."
    validations:
        required: true

  - type: textarea
    id: solution
    attributes:
      label: Proposed Solution(optional)
      description: |
        Suggest how to implement the addition or change. Please include prototype/workaround/sketch/reference implementation.
    validations:
      required: false

  - type: textarea
    id: other
    attributes:
      label: Additional Information(optional)
      description: |
        Any alternative solutions or features you considered, a more detailed explanation, stack traces, related issues, links for context, etc.
    validations:
      required: false

  - type: input
    id: package-version
    attributes:
      label: Pacakge version used
    validations:
      required: true

# TODO: Have an additional checkbox asking if the person is able to
#       implement a solution through a PR so that we can have some SLA with them? 
